info:
  name: logout
  type: http
  seq: 4

http:
  method: POST
  url: http://127.0.0.1:8000/api/auth/logout/
  headers:
    - name: Authorization
      value: Bearer {{access}}
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "refresh": "{{refresh}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
